# React Chat 

In the project directory, you can run:

```sh
folder/server 

npm install
npm i -g nodemon
nodemon

```
```sh
folder/socket

npm install
nodemon

```
```sh
folder/client

npm install
npm run dev

```

